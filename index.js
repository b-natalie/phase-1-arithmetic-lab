
// Natalie Barba
// Using const since we never have to change the values below

const num1 = 31;
const num2 = 2;
const multiply = num1 * num2;

const random = Math.floor((Math.random() * 10) + 1);

const num3 = 24;
const num4 = 5;
const mod = num3 % num4;

const max = Math.max(-13, 12, 5, -34, 20);